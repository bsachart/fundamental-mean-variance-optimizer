# Stlite Dependencies (Browser Runtime)
# These packages are loaded by stlite in the browser via Pyodide

polars==0.20.3
pandas==2.1.4
numpy==1.26.2
scipy==1.11.4
altair==5.2.0

# Note: streamlit is provided by stlite itself
# No need to list it here